```{r}
packages <- c(
  "distrEx",
  "distrTeach",
  "mvtnorm",
  "LaplacesDemon",
  "lm.beta",
  "patchwork",
  "performance",
  "TeachingDemos",
  "tidyverse"
)

for (pkg in packages) {
  if (!requireNamespace(pkg, quietly = TRUE)) {
    install.packages(pkg)
  }
}
```
