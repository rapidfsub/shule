```{r}
packages <- c(
  "distrEx",
  "distrTeach",
  "mvtnorm",
  "LaplacesDemon",
  "patchwork",
  "TeachingDemos",
  "tidyverse"
)

for (pkg in packages) {
  if (!requireNamespace(pkg, quietly = TRUE)) {
    install.packages(pkg)
  }
}
```
